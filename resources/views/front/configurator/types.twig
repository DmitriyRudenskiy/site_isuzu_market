{% set step = 1 %}

{% extends 'front.configurator.layout' %}

{% block configurator %}
    <div class="row">
        <div class="col-lg-12">
            <h2 style="margin-top: 0">Тип грузовика</h2>
        </div>

        {% for item in list %}
            <div class="col-lg-4 text-center __configurator_item" style="background-image: url('{{ item.image }}'); ">
                <p>{{ item.title | raw }}</p>
                <a href="{{ route("front_configurator_chassis", {"alias": item.id}) }}" class="btn-red-all">Следующий шаг</a>
            </div>
        {% endfor %}
    </div>
{% endblock %}
